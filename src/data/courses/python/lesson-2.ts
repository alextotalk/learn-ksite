import { Lesson } from "../types";

export const pythonLesson2: Lesson = {
  id: "python-lesson-2",
  order: 2,
  category: "python",
  topicGroup: "Типи даних",
  slug: "typy-danykh-v-python",
  title: "Python №2: Типи даних, Мутабельність (Mutable vs Immutable) та Управління Пам'яттю",
  level: "Початковий",
  duration: "25 хв",
  summary: "Фундаментальний гайд по типуванню у Python: CPython пам'ять, id(), приклади для кожного змінюваного (Mutable) та незмінюваного (Immutable) типу даних.",
  content: `У Python **все є об'єктом** (Everything is an Object) — від простих чисел до функцій та класів. Розуміння того, як Python обробляє об'єкти в пам'яті, є ключем до написання безпечного та високоефективного коду.

---

### 🧠 1. Модель пам'яті CPython та функція \`id()\`

Коли ви створюєте змінну у Python (наприклад, \`x = 100\`), ви не створюєте комірку пам'яті зі значенням, а створюєте **об'єкт у пам'яті (Heap)** та зв'язуєте з ним ім'я-змінну (ярлик).

* **\`id(object)\`**: Вбудована функція, яка повертає унікальний цілочисельний ідентифікатор об'єкта. У CPython це безпосередньо **адреса об'єкта в оперативній пам'яті**.
* **\`type(object)\`**: Повертає тип об'єкта.

\`\`\`python
a = [1, 2, 3]
b = a # b вказує на ТОЙ САМИЙ об'єкт у пам'яті!

print(id(a) == id(b)) # True
\`\`\`

---

### 🔄 2. Мутабельність (Mutable vs Immutable) з прикладами для кожного типу

Мутабельність — це здатність об'єкта змінювати свій внутрішній стан (значення) після створення у пам'яті без зміни власної адреси (\`id\`).

---

#### 🔒 Незмінювані типи (Immutable)
Після створення значення об'єкта в пам'яті змінити неможливо. Будь-яка "зміна" створює **новий об'єкт** з іншим \`id\`.

1. **\`int\` (ціле число)**:
\`\`\`python
num = 10
old_id = id(num)
num += 1 # створює новий int об'єкт 11
print(old_id == id(num)) # False!
\`\`\`

2. **\`float\` (дійсне число)**:
\`\`\`python
price = 19.99
old_id = id(price)
price += 0.01 # новий float об'єкт
print(old_id == id(price)) # False!
\`\`\`

3. **\`complex\` (комплексне число)**:
\`\`\`python
z = 2 + 3j
old_id = id(z)
z += 1j # новий complex об'єкт
print(old_id == id(z)) # False!
\`\`\`

4. **\`bool\` (логічний тип)**:
\`\`\`python
is_active = True
old_id = id(is_active)
is_active = not is_active # посилання на об'єкт False
print(old_id == id(is_active)) # False!
\`\`\`

5. **\`str\` (рядок)**:
\`\`\`python
text = "Hello"
old_id = id(text)
text += " World" # новий str об'єкт
print(old_id == id(text)) # False!
\`\`\`

6. **\`tuple\` (кортеж)**:
\`\`\`python
coords = (50.45, 30.52)
old_id = id(coords)
coords += (100.0,) # конкатенація створює новий tuple
print(old_id == id(coords)) # False!
\`\`\`

7. **\`frozenset\` (незмінювана множина)**:
\`\`\`python
fs = frozenset([1, 2, 3])
old_id = id(fs)
fs = fs.union([4]) # union повертає новий frozenset
print(old_id == id(fs)) # False!
\`\`\`

8. **\`bytes\` (байтовий рядок)**:
\`\`\`python
b_data = b"Hello"
old_id = id(b_data)
b_data += b" World" # новий bytes об'єкт
print(old_id == id(b_data)) # False!
\`\`\`

---

#### 🔓 Змінювані типи (Mutable)
Вміст об'єкта можна модифікувати (додавати, видаляти або замінювати елементи) "на місці" (in-place) без зміни його \`id\`.

1. **\`list\` (список)**:
\`\`\`python
numbers = [1, 2]
old_id = id(numbers)
numbers.append(3) # модифікація на місці
print(old_id == id(numbers)) # True! (Адреса пам'яті не змінилася)
\`\`\`

2. **\`dict\` (словник)**:
\`\`\`python
user = {"name": "Олексій"}
old_id = id(user)
user["role"] = "admin" # додавання ключа на місці
print(old_id == id(user)) # True!
\`\`\`

3. **\`set\` (множина)**:
\`\`\`python
tags = {"python", "fastapi"}
old_id = id(tags)
tags.add("postgres") # додавання елемента на місці
print(old_id == id(tags)) # True!
\`\`\`

4. **\`bytearray\` (змінюваний байтовий масив)**:
\`\`\`python
ba = bytearray(b"hello")
old_id = id(ba)
ba[0] = ord('H') # зміна першого байта на місці
print(old_id == id(ba)) # True!
\`\`\`

---

### 📊 3. Порівняльна таблиця основних типів даних

| Тип даних | Мутабельність | Специфікація | Застосування |
| :--- | :---: | :--- | :--- |
| **\`int\` / \`float\`** | Immutable | Числові значення довільної точності | Розрахунки, лічильники |
| **\`str\`** | Immutable | Незмінювана послідовність Unicode-символів | Текст, ідентифікатори |
| **\`tuple\`** | Immutable | Впорядкована фіксована послідовність | Координати, повернення кількох значень з функції |
| **\`list\`** | Mutable | Динамічний впорядкований масив | Списки елементів з можливістю додавання/видалення |
| **\`dict\`** | Mutable | Хеш-таблиця (пари ключ: значення) | Швидкий пошук за унікальним ключем |
| **\`set\`** | Mutable | Непослідовна колекція унікальних елементів | Видалення дублікатів, математичні множини |

---

### ⚡ 4. Важливі практичні нюанси

#### Чому \`list\` не може бути ключем у словнику (\`dict\`)?
Ключі у словнику повинні бути **хешованими** (об'єкти, які мають незмінне значення хешу протягом життя). Оскільки змінювані типи (\`list\`, \`dict\`, \`set\`) можуть змінювати свій вміст, їхній хеш не є стабільним. 

Спроба зробити \`{ [1, 2]: "data" }\` викличе помилку **\`TypeError: unhashable type: 'list'\`**. Натомість використовуйте \`tuple\`: \`{ (1, 2): "data" }\`.

#### Передача аргументів у функції (Pass by Assignment)
У Python аргументи передаються **за посиланням на об'єкт**:
* Якщо передати **Mutable** (\`list\`, \`dict\`) і змінити його у функції, зміни відобразяться і зовні!
* Якщо передати **Immutable** (\`int\`, \`str\`), модифікація створить новий локальний об'єкт і не змінить зовнішню змінну.`,
  codeExample: {
    language: "python",
    code: `from typing import List, Dict, Tuple

# 1. Демонстрація id() та мутабельності
user_ids: List[int] = [101, 102, 103]
initial_memory_address = id(user_ids)

# Модифікуємо список "на місці"
user_ids.append(104)
print(f"Адреса пам'яті до та після append(): {initial_memory_address == id(user_ids)}") # True

# 2. Immutable tuple як ключ для словника
locations: Dict[Tuple[float, float], str] = {
    (50.4501, 30.5234): "Київ (Центр)",
    (49.8397, 24.0297): "Львів (Площа Ринок)",
}

target_point = (50.4501, 30.5234)
print(f"Локація за координатами {target_point}: {locations.get(target_point)}")

# 3. Передача Mutable об'єкта у функцію
def add_admin_role(roles: List[str]) -> None:
    """Модифікує список ролей безпосередньо в пам'яті."""
    if "admin" not in roles:
        roles.append("admin")

current_roles = ["developer", "editor"]
add_admin_role(current_roles)
print(f"Оновлені ролі користувача: {current_roles}") # ["developer", "editor", "admin"]`
  },
  quiz: {
    question: "Чому списки (list) не можна використовувати як ключі у словниках (dict) у Python?",
    options: [
      "Тому що списки займають занадто багато пам'яті",
      "Тому що списки є змінюваними (Mutable) і не підтримують стабільний хеш (Unhashable)",
      "Тому що словники працюють тільки з цілими числами",
      "Списки насправді можна використовувати як ключі"
    ],
    answerIndex: 1,
    explanation: "Ключі словника у Python повинні мати стабільний хеш-код протягом усього життя об'єкта. Оскільки списки є мутабельними, їхній вміст (і хеш) може змінюватися, що призведе до збою хеш-таблиці. Натомість слід використовувати незмінювані кортежі (tuple)."
  }
};
